name: 🔀 Test Pipeline

on:
  push:
    branches-ignore:
      - 'release-please--branches--main'
    tags:
      - "v*.*.*"
  pull_request:
    branches:
      - 'main'

permissions:
  contents: write
  pull-requests: write
  packages: write

jobs:

  test-pipeline:
    name: ▶️ Run CI pipeline
    runs-on: ubuntu-latest
    needs:
      - pipeline